# Plan: Update `scripts/hotkey.py` to use relative paths for `TARGET_DIR` and make prefixes configurable

## Tasks
- [x] Create `plan.md`
- [x] Modify `scripts/hotkey.py` to use `BASE_DIR` for `TARGET_DIR`
- [x] Make file prefixes (input/output) configurable via constants
- [x] Make `SOURCE_DIR` a configurable constant
- [x] Add type hints and docstrings to functions
- [x] Improve error handling for file operations
- [x] Add comment explaining numeric padding configuration
- [x] Verify functionality
